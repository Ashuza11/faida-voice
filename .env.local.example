# Neon Postgres — pooled connection, used at runtime
DATABASE_URL=

# Neon Postgres — unpooled connection, required for seeding/migrations (CLAUDE.md §7)
DATABASE_URL_UNPOOLED=

# ASR provider key — server-side only, never exposed to the client (CLAUDE.md §3.9)
ASR_API_KEY=
